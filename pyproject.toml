[project]
name = "idm-book"
version = "0.1.0"
description = "Infectious disease modeling book"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[dependency-groups]
docs = [
    "pymdown-extensions>=10.16.1",
    "zensical>=0.0.24",
]
